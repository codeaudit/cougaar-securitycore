<?xml version="1.0"?> 
 
<!-- 
  An example hosts file. 
 
  Replace "foo.com" with your host name. 
--> 
<society name='foo.com-hosts'  
  xmlns:xsi='http:/www.w3.org/2001/XMLSchema-instance' 
  xsi:noNamespaceSchemaLocation='http:/www.cougaar.org/2003/society.xsd'> 
 
  <host name='fig'> 
    <facet service="NFS"/> 
  </host> 
  <host name='peach'> 
    <facet service="SMTP"/> 
    <facet service="jabber"/> 
  </host> 
  <host name='ash'> 
    <facet service="operator"/> 
  </host> 
<!-- hosts for ul -->
  <!-- ROOT CA -->
  <host name='plum'> 
    <facet service="acme"/> 
  </host> 
  <!-- CONUS CA -->
  <host name='yew'> 
    <facet service="acme"/> 
  </host> 
  <!-- CONUS REDUNDANT CA -->
  <host name='manzanita'> 
    <facet service="acme"/> 
  </host> 
  <!-- OSD.GOV -->
  <host name='balboa'> 
    <facet service="acme"/> 
  </host> 
  <!-- AMMOS TRANSCOM -->
  <host name='mango'> 
    <facet service="acme"/> 
  </host> 
  <!-- CONUS TRANSCOM -->
  <host name='lemon'> 
    <facet service="acme"/> 
  </host> 
  <!-- EURO TRANSCOM -->
  <host name='apricot'> 
    <facet service="acme"/> 
  </host> 
  <!-- REAR CA -->
  <host name='tea'> 
    <facet service="acme"/> 
  </host> 
  <!-- REAR REDUNDANT CA -->
  <host name='redwood'> 
    <facet service="acme"/> 
  </host> 
  <!-- REAR A -->
  <host name='chestnut'> 
    <facet service="acme"/> 
  </host> 
  <!-- REAR B -->
  <host name='eucalyptus'> 
    <facet service="acme"/> 
  </host> 
  <!-- REAR C -->
  <host name='birch'> 
    <facet service="acme"/> 
  </host> 
  <!-- 1-AD-DIVSUP CA --> 
  <host name='pear'> 
    <facet service="acme"/> 
  </host> 
  <!-- 1-AD-DIVSUP REDUNDANT CA --> 
  <host name='fir'> 
    <facet service="acme"/> 
  </host> 
  <!-- 1-AD-DIVSUP -->
  <host name='cypress'> 
    <facet service="acme"/> 
  </host> 

  <!-- ul2 -->
  <!-- 1-AD-DIV CA -->
  <host name='almond'>
    <facet service="acme"/>
  </host>
  <!-- 1-AD-DIV RCA -->
  <host name='cedar'>
    <facet service="acme"/>
  </host>
  <!-- 1-AD -->
  <host name='citrus'>
    <facet service="acme"/>
  </host>
  <!-- 123-MSB -->
  <host name='beech'>
    <facet service="acme"/>
  </host>
  <!-- 1-AD-BDES CA -->
  <host name='cherry'>
    <facet service="acme"/>
  </host>
  <!-- 1-AD-BDES RCA -->
  <host name='olive'>
    <facet service="acme"/>
  </host>
  <!-- 1-AD-BDES -->
  <host name='hemlock'>
    <facet service="acme"/>
  </host>

  <!-- MACHINES FOR USE -->
  <host name='pecan'>
    <facet service="acme"/>
  </host>
  <host name='balsam'>
    <facet service="acme"/>
  </host>

  <!-- MACHINES FOR PING -->
  <!-- TRANS CA -->
  <!-- TRANS REDUNDANT CA -->
  <host name='corn'> 
    <facet service="acme"/> 
  </host>
  <host name='rose'> 
    <facet service="acme"/> 
  </host>
</society> 

