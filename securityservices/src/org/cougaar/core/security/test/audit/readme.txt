To test AuditLogger for user actions, add the AuditTestServlet to an Agent

Request the servlet, whose path is /testAuditServlet

The result printed to the screen is a simple "TRUE" or "FALSE" whether it found the audit record for the user action just performed while trying to access this servlet